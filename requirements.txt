# Kairos — Python dependencies
# Pinned loosely (major.minor) — tighten exact versions once Phase 3 build starts
# and you know what's actually compatible in your environment.

# Data
pandas>=2.0
numpy>=1.24

# Database
SQLAlchemy>=2.0
psycopg2-binary>=2.9
pgvector>=0.2          # Python client for the pgvector extension

# ML / XAI
scikit-learn>=1.4
xgboost>=2.0            # or lightgbm>=4.0 — pick one per Kairos_TechStack_Evaluation_Lock.md
shap>=0.45
optuna>=3.6
mlflow>=2.12
scipy>=1.12              # PSI / KS-test drift monitoring

# Embeddings / RAG (Module B, Phase 5+)
sentence-transformers>=2.6
langchain>=0.2            # or drop for a lightweight custom orchestrator — see tech stack lock

# Backend API
flask>=3.0
flask-sqlalchemy>=3.1
flask-jwt-extended>=4.6
flask-smorest>=0.44        # or flask-restx — pick one per tech stack lock

# Testing / Dev
pytest>=8.0
python-dotenv>=1.0

# Evaluation (Phase 7)
ragas>=0.1
